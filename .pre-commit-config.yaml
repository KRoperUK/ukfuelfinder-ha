# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
#
# Stages:
# * ``pre-commit`` — fast checks (ruff, mypy, manifest sort). Run on every commit.
# * ``pre-push`` — the full pytest suite with coverage. Runs once per push so a
#   commit-heavy local workflow isn't slowed down by re-running tests on each fixup.
#
# Enable both stages once with:
#   pre-commit install --hook-type pre-commit --hook-type pre-push
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Keep in sync with the ruff pin in requirements_test.txt and ci.yml.
    rev: v0.16.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      # Keep manifest.json keys in the order hassfest expects (domain/name first,
      # then alphabetical) so CI's hassfest validation never trips on ordering.
      - id: sort-manifest
        name: sort manifest.json
        entry: python scripts/sort_manifest.py
        language: system
        files: ^custom_components/ukfuelfinder/manifest\.json$
        pass_filenames: false

      # Strict mypy on the whole integration. Runs once per commit regardless of
      # which files staged — mypy's cross-file inference means a change in one
      # module can surface an error in another.
      - id: mypy
        name: mypy (strict)
        entry: .venv/bin/mypy
        language: system
        pass_filenames: false
        # Any Python source touched should trip mypy; the config in pyproject.toml
        # narrows scope to custom_components/ukfuelfinder.
        files: \.py$
        stages: [pre-commit]

      # Full pytest run on push so no broken commit reaches origin. The suite is
      # small enough to run with coverage, so the [tool.coverage.report]
      # fail_under floor is enforced before CI ever sees the branch.
      - id: pytest
        name: pytest (with coverage)
        entry: .venv/bin/python -m pytest tests/ --cov=custom_components/ukfuelfinder --cov-report=term-missing -q
        language: system
        pass_filenames: false
        stages: [pre-push]
